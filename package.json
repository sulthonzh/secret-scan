{
  "name": "secret-scan",
  "version": "1.2.1",
  "description": "Scan files for hardcoded secrets — API keys, tokens, passwords, private keys. Zero deps.",
  "main": "src/scanner.js",
  "bin": {
    "secret-scan": "src/cli.js"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 test/*.test.js",
    "test:coverage": "c8 --include 'src/**/*.js' node --test --test-concurrency=1 test/*.test.js",
    "lint": "eslint .",
    "prepublishOnly": "npm test && npm run lint"
  },
  "keywords": [
    "secret",
    "security",
    "scan",
    "leak",
    "detection",
    "ci",
    "audit"
  ],
  "author": "sulthonzh",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "c8": "^10.1.3",
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "src/",
    "README.md",
    "CHANGELOG.md"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/sulthonzh/secret-scan.git"
  },
  "homepage": "https://github.com/sulthonzh/secret-scan#readme",
  "exports": {
    ".": {
      "require": "./src/scanner.js",
      "default": "./src/scanner.js"
    },
    "./cli": "./src/cli.js"
  },
  "publishConfig": {
    "access": "public"
  }
}
